如果已经提供了“连接页面操作”，就不用再连服务器了，直接jupyter lab（注意别点右上角的电源键，否则环境直接退出了）；
如果写了“直接访问，无须终端执行jupyter notebook”，那就直接打开对应网站就行
外网IP:8888
密码（可能会变）：
Qingjiao@qaz#123

nohup jupyter lab &
输出会自动保存到 nohup.out 下

把源文件修改下名字，然后直接上传jupyter文件，改成对应的名字，一键开启验证就完事了

自己写的话，要先保存才能过验证，有时候不用执行也能通过